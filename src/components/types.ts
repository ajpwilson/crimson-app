export type Image = {
  source: string;
  width: number;
  height: number;
}

export type GalleryPhoto = {
  src: string;
  width: number;
  height: number;
}

export type CarouselPhoto = {
  source: string;
}
